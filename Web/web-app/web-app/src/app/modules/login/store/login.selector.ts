// import { createSelector } from '@ngrx/store';
// import { selectLoginFeature as getState } from './login.reducer';

// const items = createSelector(getState, (state) => state.items);

export const LoginSelectors = {
};
